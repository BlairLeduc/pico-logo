# Bugs

Tracks defects in Pico Logo — past, present and future. A **bug** here is
behaviour that contradicts
[`reference/Pico_Logo_Reference.md`](../reference/Pico_Logo_Reference.md),
silently loses or corrupts data, or hangs/crashes the interpreter.

Sibling document: [`roadmap.md`](roadmap.md) tracks
*features* — new primitives, designs, milestones and performance work. If an
item adds capability it belongs there; if it means "this doesn't work as
documented", it belongs here.

**Status legend:** `open` · `in progress` · `fixed` · `won't fix`

**Severity:** `high` (data loss, crash, hang, unusable feature) · `medium`
(surprising wrong behaviour with a workaround) · `low` (cosmetic or narrow
edge case)

---

## Open

| ID | Bug | Area | Severity | Found | Status |
|---|---|---|---|---|---|
| [B6](#b6--penreverse-ignores-pen-size-always-1-px) | `penreverse` ignores pen size (always 1 px) | graphics | low | 2026-07-18 | won't fix (documented) |

### B6 — `penreverse` ignores pen size (always 1 px)

`penreverse` toggles pixels, so overlapping thick stamps would toggle shared
pixels twice and speckle the line. Thick reverse drawing is therefore left at
1 px and documented as a limitation rather than implemented.

- **Status:** won't fix for now. A future thick-reverse pass would delta-stamp
  only the pixels the previous stamp did not cover, or fill per-scanline spans.
- **Found:** 2026-07-18, with `setpensize` / `pensize`.

---

## Fixed

| Date | Bug | Area | Fix | Ref |
|---|---|---|---|---|
| 2026-08-10 | Pausing Galaxian or Invaders stopped the aliens but not the player | games | Reported from play: `P` froze the convoy while the ship still steered. `toggle.pause` calls `freeze`, which suspends demons and autonomous turtle motion — that is the whole of the alien side, so it looked right — but steering and firing are neither: they are `poll.input` writing to turtle 0 directly with `move.ship`/`move.cannon` and `fire`. `play.frame` deliberately calls `poll.input` *outside* its `if not :paused` guard, since otherwise a paused game could never read the key that unpauses it, so every key stayed live. Firing was live too, which is worse than it sounds: a shot launched during a pause is placed at the muzzle and only starts travelling on the thaw, so a player could pause, line the ship up and have the shot waiting. `poll.input` in both games now takes the pause key first and returns, then returns again if `:paused`, so a paused game answers `P` and nothing else. Turtle Trails has the same `P` but never had the defect — its arrow keys only record an intent (`.setitem 1 :a.next`) that the movement code, which is inside the paused guard, simply never consumes. `test_pause_holds_the_ship_as_well_as_the_aliens` and its Invaders twin pin all three halves: no steering and no firing while paused, `P` still lifts the pause, and steering works again afterwards | |
| 2026-08-10 | Trails + p10prof hold 125 of 128 globals (B14) | interpreter/games | `test_p10prof_profiler_runs` requires the game and the frame profiler together to leave eight free slots, and they held 125 of 128. Fixed from both ends. Trails' six text-colour globals from commit `061e878` (`tc.text`, `tc.bg`, `tc.dart`, `tc.swoop`, `tc.echo`, `tc.moss`) are gone: they are `settc` slot numbers used in exactly two procedures, `setup.palette` and `attract.screen`, and are now literals there, which also drops the four `(list :tc.x :tc.bg)` allocations the legend made per call. That puts the count back at 119, where it was before the attract screen was written. `MAX_GLOBAL_VARIABLES` also went 128 → 192, because 119 was already uncomfortably close and the margin is the whole point — it is what lets the profiler, `p9trails`, or any non-empty workspace load on top of a game on a board. Measured on `pico+2w` firmware, that costs exactly 1 KB: a slot is a 16-byte `Variable`, so the table goes 2048 → 3072 bytes of `.bss`, RAM 479,412 → 480,436 of 512 KB (91.44% → 91.64%), `.text` unchanged, leaving ~43 KB free. It costs no read time: `find_global` scans `global_count`, not the bound, so a lookup costs what the workspace holds. The paths that do walk the whole array are `variables_init`, once at startup, and creating a global that does not exist yet, once per name — neither is on a frame loop's path. Trails + p10prof now hold 119 of 192 | |
| 2026-08-10 | Galaxian died with "Out of space in arm.demons" after Invaders had run | games | Reported from hardware: the attract screen and the convoy both drew, then the game stopped on `arm.demons`. Nothing disarms a demon when a program ends, and Galaxian's eight collision pairs fill `MAX_DEMONS` (8) *exactly*, so it can only arm them if it owns the whole table. Invaders leaves five armed; four of Galaxian's eight match one of those by condition and are replaced in place, but the remaining four need four free slots and only three are left, so the eighth `when` failed. `arm.demons` is the tail of `setup.level`, which is why the convoy was already on the screen when it died. The order also matters the other way — Galaxian first fills all eight and Invaders' fifth `when` then has nowhere to go — and inheriting a foreign demon is its own hazard: Invaders' `[(alien.bottom.y) < -130]` would have called `invasion` inside a Galaxian whose globals it does not have. Both games now take the table back with `cleardemons` at the top of `arm.demons`, which costs nothing on the per-level re-arm since `when` already replaces a demon whose condition it matches. `test_arm_demons_takes_a_table_an_earlier_program_left_behind` in both test files strands foreign demons and pins that `setup.level` still runs and that the stranger is gone | |
| 2026-08-10 | Turtle Trails' HUD rows were laid over the board's side walls | games | Reported from hardware: the leftmost spare life sat in the hedge. Both HUD rows were laid out from the outermost column centre — lives from x −100, history from x 100, which are the centres of columns 2 and 27 — but a stamp is 16 pixels square against an 8-pixel cell, so each end stamp hung four pixels into column 1 or 28, the board's side walls, and the band that erases the row then painted that bite black. The rows now start a stamp's half-width clear of their own wall: lives run −96 → −32 and history 96 → 0, the same inset on both sides, which is the outermost position at which a 16-wide shape still clears an 8-wide wall. The right-hand overlap was the same defect mirrored and was fixed with it, though only the left was visible when it was reported. `test_the_hud_rows_clear_the_board_walls` fills both rows to capacity and pins every stamp *and* the erase band inside the walls, reading the wall positions from the game's own `tile.x` so it cannot drift from the board | |
| 2026-08-06 | Turtle Trails' HUD rows never erased a whole stamp | games | Found while fixing the sprite-anchor entry below, and independent of it. `draw.lives` and `draw.history` take the old row away by painting `:c.bg` over it before stamping the new one, but both bands were drawn at `setpensize 8` against stamps 16 pixels square — so the band was half the height it needed and a lost life left the top of its turtle on screen. `draw.history`'s band was short in the other axis too: it ran from x 20 to 100, five slots' worth, while the row holds seven and reaches down to x 4, so the two leftmost entries could never be erased. Both bands are now pen 16 and span the row's *capacity* rather than the count drawn this time (lives −100→−36, history 4→100), which with a pen's filled disc covers each row's footprint exactly and no more; both put turtle 7 back to its resting pen 8, which the rest of the file assumes. The sprite-anchor fix moved the stamps from screen rows 281–296 to 288–303, so they now sit squarely on the two bottom board rows the HUD owns, which is what makes an exact band possible. `test_the_hud_bands_erase_the_whole_stamp` fills both rows to capacity and pins every stamp's 16×16 footprint inside the band its drawer painted | |
| 2026-08-06 | `setrot "full` moved a bitmap shape 7 pixels down the screen | graphics/device | Reported from play: Turtle Trails' bugs sat a half-sprite high — the turtle's centre was on the path centre, the bugs' feet were. Both are placed on the same tile centre by `place.actor`, so the game was never at fault. `turtle_sprite_geometry` (`devices/picocalc/picocalc_console.c`) centred shape 0 and colour costumes on the turtle but anchored a mono `putsh` bitmap's *bottom row* there, and it selected between the two by testing the raster's size against 16×16 as a proxy for "plain mono bitmap". `setrot "full` rotates into a `ceil(sqrt(16²+16²))` = 23×23 square, so the proxy failed and the same bitmap silently took the centred branch instead — 7 pixels lower. The turtle is `"full` (one walk cycle serves four headings) and the bugs are `"fixed`, so the two never lined up. The same 7-pixel step also hit a bug regrowing from its `"full` wings into its `"fixed` body, the `"fixed` bonus, the turtle's `"fixed` death shell, and the three side-by-side sprites in `demos/graphics`. Every costume now centres on the turtle, which is what `snapsh` already documents for the colour case and is the anchor the rotated path had been using all along; the bottom-row anchor is gone, so no rotation style changes where a shape sits. Not reachable from the host tests — the mock device models no sprite geometry — so this is a hardware verification. All-`"fixed` programs (`galaxian`, `invaders`, the Trails HUD stamps) move down 7 pixels as a block, relative layout unchanged | |
| 2026-08-06 | A bug walking head-on into the Turtle Trails turtle passed through it | games | Reported from play: a bug coming straight at the turtle while the turtle drove at it met, and neither died. Collision was a same-tile comparison run after all five actors had moved, and two actors closing from adjacent tiles exchange tiles within one frame — the bug ends on the tile the turtle started from and the turtle on the bug's — so their centres never share a tile at the moment the test looks. The design called this out as deliberate ("no swept test") and a test pinned the pass-through; the play report overrules both, since walking into a bug is the most ordinary way the game is ever lost. `snap.tiles` now records every actor's tile at the top of `play.frame`, before anything moves, and `check.collisions` also fires on an exchange (`swapped.tile? `). An exchange can only happen by crossing, including through a tunnel where the two edge columns are adjacent, and following another actor onto the tile it just left is not one, so no swept geometry and no new false positives: `test_opposite_direction_tile_swap_collides`, `test_dizzy_bug_is_eaten_on_a_tile_swap` and `test_a_bug_following_the_turtle_does_not_collide` pin all three. The snapshot is one ten-slot list (columns 1–5, rows 6–10) because `test_p10prof_profiler_runs` caps trails plus the profiler at 120 of the interpreter's 128 global names and it was already at 118 — and it is written only with values read straight out of `a.col`/`a.row`: the first attempt stored a computed `col * 64 + row` key, which retained ~2.5 cells a frame that `recycle` could not give back and failed `test_the_frame_loop_reclaims_what_it_spends`. A computed *index* is free, since that number is only read | |
| 2026-08-06 | A regrown bug in Turtle Trails kept the wings costume | games | Second half of the wings report below, and independent of it: with the movement fixed, the bug reached the nest, regrew and left again as a *bug*, but still wearing the wings. `dress.bug` arms the body frames with `setanim`, and `dress.bugs` runs it for all four bugs every frame; `turtle_set_anim` zeroes the phase accumulator on every call, so at 25 fps the 140 ms frame interval was never reached and the animation never advanced. No bug in the game has ever animated. It only *showed* on a regrown bug because the animation frames are the one thing that ever puts the body back on — the wings branch sets its shape with `setsh`, the body branch does not, it waits for the first animation tick that never comes. `dress.bug` now only puts on a costume the turtle is not already wearing (a new `body?` reads `shape`, so no new global is spent), and sets the shape explicitly when it does; only the pen colour, which flashes while dizzy, is still written every frame. `test_a_regrown_bug_puts_its_body_back_on` drives the mock clock through a second of frames after a regrow and pins the shape back in the body range | |
| 2026-08-06 | Turtle Trails' `READY` erase took the pen trail with it | games | The first fix for the entry below repaired the fifteen cells `READY` covers from the tile map, which removed the word but also every pen trail crossing those cells — the map holds the corridor, not what the turtle drew on it. `ready.screen` now photographs the cells into spare bank slots (21–35) with `snaptile` before writing the word and stamps them back afterwards, so the corridor and the trail both survive. `stamptile` draws the slot the *map* holds, so a cell is put back by lending it the snapped slot for one stamp and handing the map its own slot back. `test_ready_puts_back_the_trail_it_covered` paints a different colour in each of the fifteen cells, runs the save, scribbles over them the way the glyphs would, runs the restore, and pins every cell back to its own colour — a photograph put back in the wrong cell cannot pass — then pins the whole screen unchanged through `ready.screen` | |
| 2026-08-06 | An eaten bug in Turtle Trails flew the maze as wings for the rest of the life | games | Reported from play: an eaten bug never changed back to a bug shape and wandered as wings. The costume was never the problem — `dress.bugs` runs every frame and follows the state — the bug never left state 5. `bug.arrive` commits the exit chosen a tile earlier, which is open by construction, but `eat.bug` clears `:a.next`, so the wings set off on the heading the bug died on with nothing committed for the tile it arrives at next. If that heading faces a hedge there, `move.actor` zeroes the step every frame, no tile centre is ever crossed, `bug.arrive` never runs again, and the wings sits (or, arriving mid-corridor, runs to the next wall and sits) until the life ends — `wings.home?` is only reachable from `bug.arrive`. A bug eaten one tile before a corner hits it. `bug.arrive` now checks `can.go?` after committing and, when the heading is blocked, chooses again from the tile it stands on: `choose.at` was split out of `choose.dir` so both the dizzy and the hunting choice are made under the same rules from either tile. `test_wings_home_from_every_tile_and_heading` sweeps every walkable tile and legal heading — 100 of them never reached the nest before the fix, none after | |
| 2026-08-06 | Turtle Trails punched `READY` out of the pen trail after a respawn | games | `ready.screen` took the word away by writing it a second time in the background colour, the same trick `draw.hud` uses for the old score. That works over the dead rows above and below the maze, where nothing but the HUD is drawn; `READY` sits on the corridor below the nest, which the turtle has driven a trail through, so the erase punched a `READY`-shaped hole in the trail that stayed for the rest of the level. The word is now taken away by repairing the fifteen cells it covers from the map (`stamp.tile`), which is also how the blossom erase already works. Its position is derived from `tile.x`/`tile.y` so the write and the repair cannot drift apart, and the two landmarks are spelled out in the procedure rather than made globals — `test_p10prof_profiler_runs` caps trails plus the profiler at 120 of the interpreter's 128 global names, and it was already at 118. Nothing in those cells is paintable, so the trail the repair takes with it carries no score. `test_ready_is_taken_away_by_repairing_its_cells` stages a trail band across the maze, runs `ready.screen`, and pins that the word is written once (never in the background colour) and that exactly the cells its glyph box falls in are put back | |
| 2026-08-06 | Galaxian carried a mid-flight diver into the next level | games | Same defect as the Turtle Trails respawn entry below, found while fixing it: `setup.level` assigned `diver.cell`, `diver.phase`, `diver.timer` and `ppos` as bare list literals, which the frame loop's `.setitem`s then mutated permanently. A level cleared while a diver was airborne began the next one with `diver.phase` still non-zero, so `all.divers.idle?` stayed false and `try.launch.dive` could not launch — with the flying turtle hidden by `setup.turtles`, that slot was lost for the rest of the game. All four now use `(list ...)`; `test_setup_level_gives_the_divers_fresh_state` marks a diver airborne, runs `setup.level` again and asserts the whole slot clears. Invaders was checked and is clean: its only literals are the read-only `freqs` and the `[]` seeds in `make.ones`. | |
| 2026-08-06 | Turtle Trails carried the previous life's bug state through a respawn | games | `place.actors` reset `a.dir`, `a.off`, `a.next`, `a.state` and `a.rev` by assigning bare list literals. A literal belongs to the procedure's stored body, and the frame loop's `.setitem`s mutate it in place, so after the first call those five lists were permanently scribbled on and every later `make` rebound the same objects. Only `a.col`/`a.row` — built with `list` — actually reset, which is why the bugs appeared at the right tiles but behaved wrongly: they resumed with the states, part-tile offsets and pending reversals they held when the life ended, and any left dizzy or eyes-only sat frozen. All five now use `(list ...)`; `test_place_actors_resets_every_state_list` scribbles on each list, calls `place.actors` and pins the whole starting vector. | |
| 2026-08-06 | Turtle Trails could leave every bug frozen after a lost life (B13) | games | `respawn` reset the one-frame paint pause (`tt.pause`) but not the player-controlled game pause (`tt.paused`). If a death transition occurred with the latter latched, each later `play.frame` skipped its simulation block: path bugs never walked and nest bugs never bobbed. `respawn` now explicitly clears `tt.paused`; `test_respawn_unpauses_the_bug_simulation` enters that state through `handle.death`, then verifies both a path bug and a nest bug resume moving after four frames. | |
| 2026-08-06 | A pause could turn the games' 250-frame reclaim into an every-frame `recycle` | games | `play.frame` counted frames inside `if not :paused` but called `reclaim` outside it, so a pause landing on a multiple of 250 left `remainder :frame.count 250` at zero for the whole pause and recycled on **every** paused frame — the every-frame recycle all three designs explicitly forbid, and the visible-hitch failure the timer exists to avoid. Present in Turtle Trails since its reclaim landed (2026-07-28) and inherited by Galaxian and Space Invaders on 2026-08-06. Found by automated review on PR #137; `test_a_paused_frame_never_recycles` reproduces it in both new games by leaving ~800 cells of garbage, pausing on frame 250 and asserting free `nodes` does not move (it moved by 219 before the fix). `reclaim` now sits inside the unpaused block in all three games, which is also where it belongs: a paused frame counts no frames and allocates nothing | #137 |
| 2026-08-06 | Galaxian and Space Invaders never reclaimed storage, and part of a level's state was set outside `setup.level` | games | Neither shipped game called `recycle` anywhere, and `space-invaders-design.md` claimed one ("`march.step` still calls `recycle` once per march") that the file never had — there is no `march.step` in it, and `git log -S recycle` shows none since the original commit. Measured on the mock: a frame that redraws nothing costs **zero** cells (both games mutate every list in place, so the body really is free), but a **HUD repaint costs ~14 cells** and follows every kill, and nothing returns it — a long scoring session slides towards `out of space`. Both games now have Turtle Trails' `reclaim`, recycling every 250 frames from a new `play.frame`, with `test_galaxian.c`/`test_invaders.c` pinning the free frame, the cost of a scoring one, and that the timer holds free storage flat over 600 more. Two cold-start defects fell out of factoring the frame: Invaders set `:freqs` (a constant note table) in `one.game` and its UFO warble counter only in `launch.ufo`, so a level built by `setup.level` alone — a test, a timing script, or a player calling `play.level` from the prompt — died with `freqs has no value` or `ufo.warble.ctr has no value`. Both now initialise where the rest of their kind does | |
| 2026-08-05 | `help` finds nothing for `.bootsel`, `wifi?`, `wifi.connect`, `wifi.disconnect` (B12) | help/build | `generate_help.awk` sorts `help_entries[]` with awk's `>` on strings, which under a UTF-8 locale (the user's `LANG=en_CA.UTF-8`) collates punctuation by locale rules rather than byte order: `;` sorted *before* `.bootsel`, and `wifi?` before `wifi.connect`. `help_lookup` binary-searches with `strcasecmp` (byte order), so the two misordered regions were unreachable — `help .bootsel` and the three `wifi` entries returned nothing. Under `LANG=C` the same generator produced a correct table, which is why this only showed up on some machines. Fixed by running the generator with `LC_ALL=C` in `scripts/generate_help.sh`, making the emitted order byte-wise and matching `strcasecmp`. `test_help`/`test_primitive_help_coverage` already pinned both halves of the contract and were the ones failing | |
| 2026-08-02 | `logo/games/trails` still documented B7 as a live constraint | docs | The game's header listed five interpreter rules, one of which — a call to a procedure defined in the file may not sit inside a parenthesised arithmetic expression — was **B7**, fixed 2026-08-01 — the day before. The header was never revisited when the bug was, and the design doc's §15 still said it was "worth fixing separately". Not a code defect, but a stale rule shapes code: it is why the file threads results through locals before doing arithmetic. Header and design corrected, the locals left alone as harmless. Two real rules added in its place, both found the hard way while writing P10's profiler: a `;` inside a bracketed list silently swallows the rest of a procedure (now rejected at load by `test_trails.c`, mutation-checked), and the outermost parens of `make "v (:a + 1)` are redundant *and* cost about 15-20 % of the statement, so they should not be added by habit | |
| 2026-08-02 | `dot` ignores the pen size on the PicoCalc (B11) | graphics/device | `turtle_dot` (`devices/picocalc/picocalc_console.c`) plotted a single pixel with `screen_gfx_set_point`, so `setpensize 6 dot [0 0]` put down one pixel where the same pen stamps a 6-px filled disc along any stroke — contradicting `setpensize`, which documents the disc and names `penreverse` (B6) as the *only* exception. Every other drawing path already went through `screen_gfx_line`, which stamps the disc; `dot` was the one that did not. Found in P9 M3: Turtle Trails draws its tile bank with `dot`, and every tile came back as background with a single hedge pixel in it, so the baked maze was invisible on hardware. Shipped code had been quietly affected all along — Trails' specks and blossoms were 1-px dots rather than the 2-px speck and 6-px disc `draw.specks` asked for, and the blossom erase blacked out one pixel. Fixed by drawing the dot as a zero-length line at the current pen size, which `screen_gfx_line` stamps exactly once; at pen size 1 the rounding and clipping are identical to `set_point`, so narrow dots are unchanged. The mock now records a dot's pen size (`MockDot.pen_size`) and `test_setpensize_records_on_a_dot` pins the contract — though note the defect itself was in device code no native test reaches, so the mock is the specification and hardware is the verification | |
| 2026-08-01 | A bare exponent marker silently evaluates as a number (B10) | eval | `is_number_string` (`core/eval_expr.c`) — the evaluator's third copy of the number grammar — did not require a digit after an exponent marker, so `1e`, `1n` and `1e+` passed it and `print 1e` printed `1`, while both tokenizers (the lexer's `is_valid_number`, `token_source.c`'s `is_number_word`) classify `1e` as a word and coercion rejects it (`sum "1e 1` errors). So the same spelling was a number to the evaluator and a non-number everywhere else, and a procedure named `1e` could be defined but never called. Found while reviewing the P10 throughput design (its differential-test caution, §9). Fixed by adding the digit-after-marker requirement to `is_number_string`, matching the other two copies; valid forms (`1e5`, `1e+2`, `1n2`) unchanged. Low severity — no shipped program uses a bare exponent word | |
| 2026-08-01 | Newline markers count as list elements (B9) | workspace/format | The reported symptom — `count item 5 item 2 text "mlb` giving 7 against 4 for the same body on one line — was one route into a wider defect: a multi-line `[...]` literal used as *data* carries the markers too, so `to lit output [` / `a` / `b` / `]` / `end` made `count lit` output 5. Fixing `text` alone would have left that reachable. The markers stay where they are (so `po`, `save` and `edit` still reproduce the layout exactly, and `text` → `define` still round-trips it), and the invariant is now that a marker is layout, never an element: every reader of a list's elements walks the spine with the new `mem_first_cell` / `mem_next_cell` helpers, which skip markers. That covers `first`/`last`/`butfirst`/`butlast`/`item`/`count`/`empty?`/`replace`/`member`/`pick`/`reverse`/`shuffle`/`remove`/`remdup`/`lput`/`sentence`/`.setfirst`/`.setbf`/`.setitem`, `equal?` and `value_to_string` in `value.c`, the `apply`/`foreach`/`map`/`map.se`/`filter`/`find`/`reduce`/`crossmap` cursors and their lambda-spec parser, `define`'s parameter and body walk, `for`'s control list, `local`'s name list, the `po`/`pot`/`erase`/`trace`/`step`/`save`/`edit` name lists, `json.get`/`json.stringify`, `standout`, and the `setdate`/`settime`/`putsh`/turtle-set/voice-set/`play` readers. Constructors that copy element-wise (`sentence`, `lput`, `map.se`) now drop the markers from what they build. The bracket and paren cases are covered together, since both go through the same readers | |
| 2026-08-01 | A user-procedure call as the left operand of a parenthesised expression corrupts the parse (B7) | parser/eval | The grouping-paren handler in `eval_primary` never handled deferral: when the expression inside `(...)` deferred a user proc call to the trampoline, the handler carried on synchronously and consumed a `)` that had not been reached yet. The old workaround — the deferral branch eating the following `)` itself — made `(f :x) + (g :y)` work by cancelling that mistake out, but left the operands re-associated (`(item 1 [5 6]) + (3 * (f 2))` gave 21, not 11) or a stray `)` in the stream (`pr ((f 2) * 3)` printed 6 then raised `) without (`) whenever the paren was a grouping one. Deferral is now honoured: a new `OP_PAREN_GROUP` op parks the closing `)` on the op stack and consumes it once the group has its value, and the deferral branch leaves the `)` alone. Getting the continuation into the right stack slot needed `op_stack_insert` in place of `op_stack_swap_top`: swapping only reaches one op down, so a continuation landed *inside* the ops a nested deferral had pushed and results flowed back out of order. `eval_expr_bp` inserts its `OP_EXPR_EVAL` the same way. `eval->paren_depth` is still maintained but remains write-only — nothing in the interpreter reads it | |
| 2026-08-01 | `name_buf[64]` identifier truncation aliasing (B5) | eval | The evaluator copied a word token into a 64-byte stack buffer before looking it up, so everything past the 63rd character was thrown away. The reported aliasing is real — a call to `<63 chars>.other` ran a procedure actually named `<63 chars>` — but the commoner symptom was worse: a procedure whose name is 64 characters or longer was stored under its full name (`proc_define_from_text` interns the whole word) and looked up truncated, so it could be defined but never called, and its recursive calls also fell out of tail-call detection. Fixed by removing the buffer: new `primitive_find_n` / `proc_find_n` take a pointer and a length and match only a stored name of exactly that length, and the three lookup sites (`eval_expr.c` word case, `eval_expr.c` `(proc …)` paren-call case, `eval_steps.c` tail-call lookahead) pass the token directly. `primitive_find` / `proc_find` are now one-line wrappers, so the C-string callers are unchanged. `repl.c` keeps its own `name_buf[64]`, but only to check a `to` line's name against the primitive table — no primitive name is 63 characters, so a truncated name cannot false-match there | |
| 2026-07-31 | `parse_list` silently drops tokens (B4) | parser | `parse_list` (`core/eval_expr.c`) had two silent-drop paths. The one the review named — the `else` that advanced past an unrecognised token — turned out to be unreachable: the token kinds it can receive are exhaustively handled above it, `TOKEN_COMMENT` never reaches an evaluator (no eval lexer sets `preserve_comments`, and `classify_word` never produces it), and `TOKEN_ERROR` is never produced at all (`make_error_token` is dead code). The *reachable* drop was the sibling `TOKEN_EOF` case, which ended the list quietly, so an unterminated `[` silently truncated it: `show [a b` printed `[a b]` and `show [a ; b]` printed `[a]` — the comment runs to end of line and takes the `]` with it, exactly the "a typo silently changes the list's contents" symptom. `parse_list` now returns an error code instead of a bool: new error 72 `[ without ]` at end of input, `ERR_DONT_KNOW_WHAT` for an unrecognised token, `ERR_OUT_OF_SPACE` unchanged. Two test inputs relied on the old tolerance (`define "inner [[] [run [throw "toplevel]]` is short one `]`; `define "test.comment [[] [print 42 ; trailing comment]]` has both closers inside the comment) and were corrected. No shipped `.logo` program has an unbalanced top-level line; procedure bodies go through `parse_bracket_contents`, which is untouched | |
| 2026-07-31 | Demons fire during `load` (B3) | demons | `load` evaluates each line through `eval_instruction`, which polls demons, so a `when` armed by a file ran its action while the rest of the file was still being read — inside `load`'s reentrancy guard, where the action could neither `load` nor call a procedure defined further down the same file. New `demons_suspend` / `demons_resume` hold polling off for the duration of the load (checked in `demons_poll` itself, so the device idle loop is covered too); `load` resumes before `startup` runs and polls once on its way out, so an armed demon still gets its first chance promptly. Resume clears the motion-clock baseline like `thaw`, so a moving turtle does not jump by the load's duration. No `.logo` file arms a demon at file level — every `when` is inside a procedure — so nothing depended on the old mid-load firing | |
| 2026-07-31 | Single-line `to … end` definitions are not supported (B2) | loader/REPL | Only a standalone `end` line closed a definition, so `to f  play [c d e]  end` swallowed every procedure that followed until the next lone `end`. New `repl_find_end_token` lexes the line and accepts `end` as the *last* token outside brackets and parens — so `pr [the end]` and `pr "end` stay ordinary words, and closing a definition can never discard anything else on the line. The three copies of the accumulate/close loop (REPL, `load`, editor) were replaced by one shared `repl_proc_def_append`. Reference updated: it required `end` alone on a line while its own examples used one-liners | |
| 2026-07-31 | Multi-line `(…)` expressions inside a procedure body evaluate to empty (B1) | parser | `proc_define_from_text` split the body on every newline, so `op (list` on its own line evaluated as `(list)`. It now tracks `(` nesting and, while a paren is open, keeps the tokens on the same body line. The break is recorded as a newline marker rather than dropped, so a `;` comment inside the expression still ends at its own source line and `po` reproduces the layout (`core/format.c` renders a top-level marker as a line break, `core/eval_steps.c` skips it when printing a stepped line). `end` is still recognised at line start inside an open paren, so an unbalanced `(` cannot swallow the procedures that follow it | #127 |
| 2026-07-31 | A turtle command automatically switches to splitscreen (B8) | graphics | Deleted `screen_show_field()` and its 16 call sites in the PicoCalc turtle ops, so only `textscreen`/`splitscreen`/`fullscreen` and `F1`–`F3` choose the mode. Drawing under `textscreen` already worked — `screen_gfx_blit_dirty` skips the blit in text mode and keeps the dirty state, and a later mode switch marks all dirty and presents — so the picture now waits instead of stealing the screen. Reference updated to match (it had documented the old behaviour, inherited from LCSI Logo). Validated on hardware — the host tests cannot reach the PicoCalc device layer | #126 |
| 2026-07-22 | Atom GC freed storage still reachable from roots | memory | Corrected root handling in the atom collector | `1eaa4ae`, #120 |
| 2026-07-21 | WiFi: joining unreliable — steady retrying *sustains* the AP's rejection penalty | wifi | Retry policy changed to burst-then-escalating-rests (3 attempts at 2 s, then one probe per 30/60/120 s rest); plain exponential backoff was tried first and did not help. Diagnosed from on-device `wifi.log` traces | #116 |
| 2026-07-21 | WiFi: healthy-but-slow joins torn down mid-DHCP | wifi | Stall patience raised from 8 s to 30 s (DHCP measured at ~8.4 s on the user's network) | #116 |
| 2026-07-21 | WiFi: rejoin after a drained disconnect lost its ACTIVE flag (EV_DISASSOC race) | wifi | `cyw43_wifi_leave` only *queues* the disassoc; its event handler later zeroes `wifi_join_state`. `picocalc_wifi_disconnect` now drains (polls `cyw43_wifi_link_status != CYW43_LINK_JOIN`, bounded ≤1 s) before returning | #116 |
| 2026-07-21 | WiFi: `wifi.status` re-issued the join once per poll, resetting join state ~50×/s | wifi | Status mapping made pure; a rejoin is issued only when the state has *stalled* | #116 |
| 2026-07-21 | WiFi: startup-file connect latched `failed` on the transient `CYW43_LINK_NONET` | wifi | `NONET` treated as "scan still running" and the join re-issued, mirroring the SDK's blocking loop; only `FAIL`/`BADAUTH` stay terminal | #116 |
| 2026-07-16 | HTTP: uploads ≥ ~150 KB stalled or failed with `503` | httpd | Replaced `ERR_MEM` receive backpressure (refused segments only retried on lwIP's 250 ms timer) with deferred `altcp_recved` from the read path; `lfs_stream_write_bytes` now seeks only when the position actually differs, so LittleFS stops flushing its cache every 512 B | `ab69eb6`, #108 |
| 2026-07-16 | HTTP: a 20 MB upload corrupted, stalled to `503`, then bricked the server ("Can't open http server" on re-run) | httpd | Real receive backpressure (`ERR_MEM` refuse-when-full) with the ring raised to 4 KB (> `TCP_WND`); a connection abandoned without a response is now RST-closed so its port frees with no TIME_WAIT | `04c7b4c`, #108 |
| 2026-07-16 | HTTP: `http://picologo.local` returned `408` on a real board (ping worked) | httpd | The accept path parked the new PCB and wired its recv callback ~20 ms later, and lwIP frees inbound data delivered to a PCB with a NULL recv callback. The listener now pre-allocates a connection slot in `listen` and the accept callback wires recv immediately | `7296302`, #108 |
| 2026-07-16 | Crash when a device's sound ops are NULL; note-length prefix range wrong | sound | NULL-op guard plus a corrected range check | `921ca40`, #111 |
| 2026-07-06 | Default colour values wrong in the 24-bit and 16-bit palettes | graphics | Corrected palette entries | `b72712e` |

---

## Reporting a bug

1. Write a failing test first (`tests/test_<module>.c`, mock device) that
   reproduces it — see the Unit Testing rules in
   [`CLAUDE.md`](../CLAUDE.md).
2. Add a row to **Open** above with the next `B<n>` ID and a detail section.
3. Fix it, run `ctest --preset=tests`, then move the row to **Fixed** with the
   date and PR.

Bugs that cannot be reproduced on the host (real lwIP or cyw43 timing, PSRAM,
audio DMA) should say so explicitly and record how they were validated on
hardware instead.
